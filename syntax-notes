Blocks start with ":" and end with ".".  "_" or "…" followed by an identifier is equivalent to a string of "."s.

A list is comma-separated values surrounded by parentheses.

A closure/function is a list followed by a block.

